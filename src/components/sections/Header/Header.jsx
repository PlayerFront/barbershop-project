import './_header.scss';
import Hero from './Hero/Hero';

const Header = () => {
    return (
        <header className="header">
            <Hero />
        </header>
    );
};

export default Header;